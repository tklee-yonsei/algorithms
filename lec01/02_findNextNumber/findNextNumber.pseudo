// findNextNumber.pseudo
함수 findNextNumber(앞사람의수, 내수열):
  시작
    for i = 0 to 내수열의길이-2:
      if 내수열[i] == 앞사람의수:
        다음수 = 내수열[i+1]
        다음수를 반환
    
    // 찾지 못한 경우
    "수열에서 찾을 수 없습니다" 반환
  끝