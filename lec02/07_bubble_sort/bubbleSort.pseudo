// bubbleSort.pseudo
함수 bubbleSort(배열, 배열크기):
  시작
    // 모든 패스를 수행 (배열크기-1번)
    for i = 0 to 배열크기 - 2:
      // 마지막 i개 요소는 이미 정렬됨
      for j = 0 to 배열크기 - i - 2:
        // 인접한 요소들을 비교
        if 배열[j] > 배열[j + 1]:
          // 큰 요소를 뒤로 보내기 (swap)
          배열[j]와 배열[j + 1]을 교환
  끝
