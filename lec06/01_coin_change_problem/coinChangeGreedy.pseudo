// coinChangeGreedy_pseudo
함수 coinChangeGreedy(동전배열, 금액):
  시작
    동전배열을 내림차순으로 정렬
    결과배열 = 빈배열
    
    동전배열의 각 동전에 대해:
      개수 = 금액 / 동전값
      만약 개수 > 0이면:
        결과배열에 (동전값을 개수만큼) 추가
        금액 = 금액 % 동전값
    
    결과배열과 결과배열의 길이를 반환
  끝
