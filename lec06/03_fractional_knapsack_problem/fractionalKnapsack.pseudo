// fractionalKnapsack_pseudo
함수 fractionalKnapsack(용량, 물건배열):
  시작
    물건배열을 단위무게당가치 기준으로 내림차순 정렬
    총가치 = 0
    선택된물건배열 = 빈배열
    
    물건배열의 각 물건에 대해:
      무게 = 현재물건의무게
      가치 = 현재물건의가치
      
      만약 용량 >= 무게이면:
        // 물건 전체를 가져갈 수 있는 경우
        선택된물건배열에 (무게, 가치, 1.0) 추가
        총가치 += 가치
        용량 -= 무게
      그렇지않으면:
        // 물건의 일부만 가져갈 수 있는 경우
        비율 = 용량 / 무게
        선택된물건배열에 (무게, 가치, 비율) 추가
        총가치 += 가치 * 비율
        반복문 종료
    
    선택된물건배열과 총가치를 반환
  끝
